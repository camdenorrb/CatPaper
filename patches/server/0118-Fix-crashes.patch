From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: camdenorrb <me@12oclock.dev>
Date: Fri, 18 Nov 2022 04:05:04 -0600
Subject: [PATCH] Fix crashes


diff --git a/src/main/java/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java b/src/main/java/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java
index 6930dbd215dc23449e143a56468077a375f801a7..7254b6b696d7bfa9850d17e83bcec6d4ac694de0 100644
--- a/src/main/java/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java
+++ b/src/main/java/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java
@@ -85,7 +85,7 @@ public class WalkNodeEvaluator extends NodeEvaluator {
                 i = Mth.floor(mob.getY() + 0.5D);
             } else {
                 BlockPos blockPos;
-                for(blockPos = mob.blockPosition(); (this.level.getBlockState(blockPos).isAir() || this.level.getBlockState(blockPos).isPathfindable(this.level, blockPos, PathComputationType.LAND)) && blockPos.getY() > mob.level.getMinBuildHeight(); blockPos = blockPos.below()) {
+                for(blockPos = mob.blockPosition(); (level.getBlockState(blockPos).isAir() || level.getBlockState(blockPos).isPathfindable(level, blockPos, PathComputationType.LAND)) && blockPos.getY() > mob.level.getMinBuildHeight(); blockPos = blockPos.below()) {
                 }
 
                 i = blockPos.above().getY();
@@ -93,7 +93,7 @@ public class WalkNodeEvaluator extends NodeEvaluator {
         } else {
             while(mob.canStandOnFluid(blockState.getFluidState())) {
                 ++i;
-                blockState = this.level.getBlockState(mutableBlockPos.set(mob.getX(), i, mob.getZ()));
+                blockState = level.getBlockState(mutableBlockPos.set(mob.getX(), i, mob.getZ()));
             }
 
             --i;
